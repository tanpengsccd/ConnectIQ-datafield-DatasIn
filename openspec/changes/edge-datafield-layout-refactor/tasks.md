## 1. 清理旧代码

- [x] 1.1 移除 `onLayout()` 中的 `getObscurityFlags()` 相关逻辑
- [x] 1.2 删除 `resources/layouts/layouts.xml` 中不再使用的 layout 定义
- [x] 1.3 移除手表相关的布局分支代码

## 2. 设备自适应基础

- [x] 2.1 添加 `isLargeScreen` 变量，基于 `dc.getHeight() > 200` 判断
- [x] 2.2 实现根据屏幕大小选择字体大小的方法 `getLabelFont()` 和 `getValueFont()`
- [x] 2.3 获取数据框可用尺寸：`dc.getWidth()` 和 `dc.getHeight()`

## 3. 布局算法实现

- [x] 3.1 实现单字段居中布局（1字段）
- [x] 3.2 实现双字段上下布局（2字段），支持配置决定大小顺序
- [x] 3.3 实现三字段品字布局（3字段），支持倒/正品字配置
- [x] 3.4 实现四字段田字布局（4字段）

## 4. 数据格式与单位

- [x] 4.1 实现速度格式：四舍五入取整，最大99，显示 "km/h" 单位
- [x] 4.2 实现功率格式：整数，最大9999，显示 "W" 单位
- [x] 4.3 实现心率格式：整数，最大999，显示 "bpm" 单位
- [x] 4.4 实现坡度格式：小数点后一位，显示 "%" 单位

## 5. 配置选项

- [x] 5.1 添加双字段布局顺序配置（用户设置）
- [x] 5.2 添加三字段品字方向配置（用户设置）